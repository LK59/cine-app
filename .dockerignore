node_modules
.next
.git
.env
.env.local
npm-debug.log*
# Runtime-only persistent data (SQLite db, downloaded trailer video files) — must never be
# baked into a build layer, even if a local ./data happens to exist in the build context.
data
