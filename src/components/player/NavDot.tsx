/** La pastille d'un onglet : du nouveau à lire (une réponse à un signalement). */
export function NavDot() {
  return <span aria-hidden className="absolute -right-1 -top-0.5 h-2.5 w-2.5 rounded-full bg-accent-500 ring-2 ring-ink" />;
}
